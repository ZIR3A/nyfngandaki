import Image from "next/image"; import { Container } from "@/components/layout/Container"; export function PageBanner({ title, subtitle, imageSrc, breadcrumb }) { return ( <div className="relative w-full h-[300px] md:h-[400px] overflow-hidden bg-primary/90 flex items-center"> {imageSrc && ( <Image src={imageSrc} alt={title || "Banner"} fill className="object-cover opacity-30" priority /> )} <Container className="relative z-10 space-y-4"> {breadcrumb && <div className="mb-4">{breadcrumb}</div>} <h1 className="text-4xl md:text-5xl font-bold text-white">{title}</h1> {subtitle && <p className="text-lg md:text-xl text-white/80 max-w-2xl">{subtitle}</p>} </Container> </div> ); }
