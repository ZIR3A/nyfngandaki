import { ResourceForm } from "@/features/resources/components/ResourceForm";

export const metadata = {
  title: "Create Resource | NYFN Admin",
};

export default function NewResourcePage() {
  return <ResourceForm />;
}
