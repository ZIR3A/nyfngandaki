import { EventForm } from "@/features/events/components/EventForm";

export const metadata = {
  title: "Create Event | NYFN Admin",
};

export default function NewEventPage() {
  return <EventForm />;
}
