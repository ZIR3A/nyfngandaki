import { CommitteeForm } from "@/features/admin/committees/components/CommitteeForm";

export const metadata = { title: "Add New Committee | NYFN Admin" };

export default function NewCommitteePage() {
  return <CommitteeForm />;
}
