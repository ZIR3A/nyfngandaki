import { ActivityForm } from "@/features/activities/components/ActivityForm";

export const metadata = {
  title: "Create Activity | NYFN Admin",
};

export default function NewActivityPage() {
  return <ActivityForm />;
}
