import { PositionForm } from "@/features/admin/positions/components/PositionForm";

export const metadata = { title: "Add New Position | NYFN Admin" };

export default function NewPositionPage() {
  return <PositionForm />;
}
