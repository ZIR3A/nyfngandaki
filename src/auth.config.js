export const authConfig = { session: { strategy: "jwt", maxAge: 1 * 60 * 60 }, callbacks: { async jwt({ token, user }) { if (user) { token.id = user.id; token.role = user.role; } return token; }, async session({ session, token }) { if (session.user) { session.user.id = token.id; session.user.role = token.role; } return session; } }, pages: { signIn: "/login" }, providers: [], secret: process.env.AUTH_SECRET };
